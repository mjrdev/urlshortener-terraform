# Key do state de prod. Precisa continuar exatamente com este valor: e o mesmo
# que estava fixo no bloco backend antes do backend virar parcial, entao o init
# nao dispara migracao de state.
key = "url-shortener/terraform.tfstate"
